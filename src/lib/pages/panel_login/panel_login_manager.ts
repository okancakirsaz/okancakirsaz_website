import { CoreManager } from "../../core/manager/core_manager";

export default class PanelLoginManager extends CoreManager{

init(){
    this.setNewTitle("Wait where did you find this page?");
    //TODO: Don't forget follow by robots
}
}